# 爱宝贝儿孕婴生活馆小程序 - 后端需求实现文档

## 项目概述

- **项目名称**：爱宝贝儿孕婴生活馆
- **项目类型**：微信小程序
- **核心功能**：孕婴商品销售与门店服务预约
- **目标用户**：家有0-3岁婴儿的家庭用户

---

## 一、前端功能需求与后端接口对照表

### 1.1 首页 (pages/index/index)

#### 前端功能需求
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 顶部导航 | 显示门店名称、消息入口(带未读数红点)、会员入口 | `/api/messages/unread-count` |
| 搜索框 | 支持关键词搜索、热词快捷搜索 | `/api/products?keyword=xxx` |
| 轮播图 | 展示促销活动/商品，点击跳转详情 | `/api/home/banners` |
| 限时秒杀 | 展示秒杀商品列表，显示原价/秒杀价 | `/api/home/seckill` |
| 核心品类 | 横向滚动分类图标，点击跳转商品列表 | `/api/home/categories` |
| 爆款推荐 | 2列网格展示热销商品 | `/api/home/hot-products` |
| 门店服务 | 6个服务入口图标，点击跳转服务详情页 | `/api/home/services` |
| 门店信息 | 显示地址、营业时间、电话 | `/api/store/info` |

#### 后端接口详细设计

**1. 获取首页数据 - GET /api/home**

返回数据包含：
```json
{
  "code": 200,
  "data": {
    "banners": [
      {
        "id": 1,
        "title": "春季新品上市",
        "image": "https://xxx/banner1.jpg",
        "linkType": "product",
        "linkId": 1
      }
    ],
    "seckillProducts": [
      {
        "id": 1,
        "name": "爱他美白金版奶粉",
        "image": "https://xxx/product1.jpg",
        "originalPrice": 299.00,
        "currentPrice": 199.00,
        "sales": 1258,
        "stock": 200
      }
    ],
    "categories": [
      {
        "id": 1,
        "name": "奶粉",
        "icon": "🍼"
      }
    ],
    "hotProducts": [
      {
        "id": 1,
        "name": "婴儿推车",
        "image": "https://xxx/product.jpg",
        "price": 599.00,
        "sales": 3560
      }
    ],
    "services": [
      {
        "id": 1,
        "name": "婴儿游泳",
        "icon": "🛁",
        "price": 58.00,
        "url": "/pages/service/bath/bath"
      }
    ],
    "storeInfo": {
      "name": "爱宝贝儿孕婴生活馆",
      "address": "河南省新乡市xxx",
      "phone": "13273721553",
      "businessHours": "07:30-21:00"
    }
  }
}
```

**2. 获取未读消息数 - GET /api/messages/unread-count**
```json
{
  "code": 200,
  "data": {
    "count": 3
  }
}
```

**3. 搜索商品 - GET /api/products?keyword=奶粉**
```json
{
  "code": 200,
  "data": {
    "list": [...],
    "total": 100,
    "page": 1,
    "pageSize": 20
  }
}
```

---

### 1.2 商品列表页 (pages/products/index)

#### 前端功能需求
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 分类选择 | 顶部横向滚动分类列表，点击筛选 | `/api/categories` |
| 搜索功能 | 输入关键词搜索商品 | `/api/products?keyword=xxx` |
| 综合排序 | 按销量、价格排序 | `/api/products?sort=sales` |
| 商品列表 | 网格展示商品图片、名称、价格 | `/api/products` |
| 跳转详情 | 点击商品跳转详情页 | - |

#### 后端接口详细设计

**1. 获取商品分类 - GET /api/categories**
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "name": "奶粉",
        "icon": "🍼",
        "children": [
          {"id": 11, "name": "一段"},
          {"id": 12, "name": "二段"}
        ]
      }
    ]
  }
}
```

**2. 获取商品列表 - GET /api/products**
参数：`category`, `keyword`, `filter`(hot/seckill), `sort`(price_asc/price_desc/sales), `page`, `pageSize`
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "name": "爱他美白金版奶粉",
        "image": "https://xxx/product.jpg",
        "price": 199.00,
        "originalPrice": 299.00,
        "sales": 1258,
        "stock": 200,
        "isSeckill": true,
        "isHot": true
      }
    ],
    "total": 100,
    "page": 1,
    "pageSize": 20,
    "totalPages": 5
  }
}
```

---

### 1.3 商品详情页 (pages/product-detail/index)

#### 前端功能需求
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 图片轮播 | 商品多图展示，支持预览 | `/api/products/{id}` |
| 商品信息 | 显示价格、原价、规格、销量、库存 | `/api/products/{id}` |
| 套餐选择 | 显示可选套餐及其价格 | `/api/products/{id}` |
| 数量选择 | 加减按钮选择购买数量 | - |
| 收藏功能 | 点击切换收藏状态 | `/api/favorites` POST/DELETE |
| 加入购物车 | 选规格后加入购物车 | `/api/cart/add` |
| 立即购买 | 创建订单并跳转支付 | `/api/orders` |

#### 后端接口详细设计

**1. 获取商品详情 - GET /api/products/{id}**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "name": "爱他美白金版奶粉",
    "images": [
      "https://xxx/product1.jpg",
      "https://xxx/product2.jpg"
    ],
    "detailImages": [
      "https://xxx/detail1.jpg",
      "https://xxx/detail2.jpg"
    ],
    "price": 199.00,
    "originalPrice": 299.00,
    "spec": "800g/罐",
    "stock": 200,
    "sales": 1258,
    "isSeckill": true,
    "isFavorite": false,
    "packages": [
      {"id": 1, "name": "单罐", "price": 199.00},
      {"id": 2, "name": "两罐套装", "price": 380.00}
    ]
  }
}
```

**2. 添加收藏 - POST /api/favorites**
请求：`{"productId": 1}`
```json
{
  "code": 200,
  "message": "收藏成功"
}
```

**3. 取消收藏 - DELETE /api/favorites/{productId}**
```json
{
  "code": 200,
  "message": "取消收藏成功"
}
```

**4. 添加到购物车 - POST /api/cart/add**
请求：`{"productId": 1, "packageId": 1, "quantity": 2}`
```json
{
  "code": 200,
  "message": "添加成功",
  "data": {
    "cartId": 1
  }
}
```

---

### 1.4 个人中心页 (pages/profile/index)

#### 前端功能需求
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 用户信息 | 显示头像、昵称、VIP等级、积分 | `/api/user/info` |
| 会员充值入口 | 跳转会员充值页 | `/pages/member/recharge/recharge` |
| 会员积分入口 | 跳转积分页(签到、兑换) | `/pages/member/sign/sign` |
| 我的订单入口 | 显示各状态订单数量 | `/api/orders/count` |
| 我的收藏入口 | 跳转收藏列表 | `/pages/profile/favorites` |
| 消息通知入口 | 显示未读消息数 | `/api/messages/unread-count` |

#### 后端接口详细设计

**1. 获取用户信息 - GET /api/user/info**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "username": "user123",
    "nickname": "宝宝妈妈",
    "avatar": "https://xxx/avatar.jpg",
    "vipLevel": "VIP1",
    "points": 500,
    "balance": 100.00
  }
}
```

**2. 获取订单各状态数量 - GET /api/orders/count**
```json
{
  "code": 200,
  "data": {
    "pending_payment": 2,
    "paid": 1,
    "shipped": 1,
    "completed": 5
  }
}
```

---

### 1.5 我的订单页 (pages/profile/orders)

#### 前端功能需求
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 订单tab | 待付款/待发货/待收货/已完成/已取消 | `/api/orders?status=xxx` |
| 订单列表 | 显示商品图片、名称、价格、数量、状态 | `/api/orders` |
| 订单操作 | 取消订单、确认收货、立即支付 | `/api/orders/{id}/cancel`, `/api/orders/{id}/pay` |
| 跳转详情 | 点击订单跳转详情页 | - |

#### 后端接口详细设计

**1. 获取订单列表 - GET /api/orders**
参数：`status`, `page`, `pageSize`
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "orderNo": "ORD20240129001",
        "status": "pending_payment",
        "statusText": "待付款",
        "totalPrice": 398.00,
        "totalQuantity": 2,
        "createTime": "2024-01-29 12:00:00",
        "items": [
          {
            "id": 1,
            "productId": 1,
            "productName": "爱他美奶粉",
            "productImage": "https://xxx/product.jpg",
            "packageName": "单罐",
            "price": 199.00,
            "quantity": 2
          }
        ]
      }
    ],
    "total": 10,
    "page": 1,
    "pageSize": 20
  }
}
```

**2. 取消订单 - PUT /api/orders/{id}/cancel**
```json
{
  "code": 200,
  "message": "订单已取消"
}
```

**3. 支付订单 - POST /api/orders/{id}/pay**
请求：`{"payMethod": "wechat"}`
```json
{
  "code": 200,
  "message": "支付成功",
  "data": {
    "orderId": 1,
    "orderNo": "ORD20240129001",
    "status": "paid"
  }
}
```

**4. 确认收货 - PUT /api/orders/{id}/confirm**
```json
{
  "code": 200,
  "message": "确认收货成功"
}
```

---

### 1.6 我的收藏页 (pages/profile/favorites)

#### 前端功能需求
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 收藏列表 | 显示收藏商品图片、名称、价格 | `/api/favorites` |
| 取消收藏 | 左滑或点击取消收藏 | `/api/favorites/{id}` |
| 跳转详情 | 点击商品跳转详情页 | - |

#### 后端接口详细设计

**1. 获取收藏列表 - GET /api/favorites**
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "productId": 1,
        "productName": "爱他美奶粉",
        "productImage": "https://xxx/product.jpg",
        "price": 199.00,
        "originalPrice": 299.00,
        "sales": 1258,
        "createTime": "2024-01-15 10:30:00"
      }
    ],
    "total": 10,
    "page": 1,
    "pageSize": 20
  }
}
```

**2. 取消收藏 - DELETE /api/favorites/{id}**
```json
{
  "code": 200,
  "message": "取消收藏成功"
}
```

---

### 1.7 会员充值页 (pages/member/recharge/recharge)

#### 前端功能需求
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 会员等级展示 | 显示当前VIP等级、积分余额 | `/api/member/info` |
| 充值档位 | 显示可选择的充值金额档位 | `/api/member/tiers` |
| 选择档位 | 点击选择充值档位 | - |
| 确认充值 | 调用支付接口充值 | `/api/member/recharge` |

#### 后端接口详细设计

**1. 获取会员信息 - GET /api/member/info**
```json
{
  "code": 200,
  "data": {
    "vipLevel": "VIP1",
    "points": 500,
    "balance": 100.00,
    "nextLevelPoints": 500,
    "benefits": ["9.5折优惠", "生日双倍积分", "专属客服"]
  }
}
```

**2. 获取充值档位 - GET /api/member/tiers**
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "name": "VIP1",
        "amount": 100.00,
        "bonus": 10.00,
        "vipLevel": "VIP1",
        "pointsBonus": 100
      },
      {
        "id": 2,
        "name": "VIP2",
        "amount": 500.00,
        "bonus": 75.00,
        "vipLevel": "VIP2",
        "pointsBonus": 600
      },
      {
        "id": 3,
        "name": "VIP3",
        "amount": 1000.00,
        "bonus": 200.00,
        "vipLevel": "VIP3",
        "pointsBonus": 1500
      }
    ]
  }
}
```

**3. 会员充值 - POST /api/member/recharge**
请求：`{"tierId": 1}`
```json
{
  "code": 200,
  "message": "充值成功",
  "data": {
    "transactionNo": "RC20240129001",
    "amount": 100.00,
    "bonus": 10.00,
    "totalAmount": 110.00,
    "balance": 210.00,
    "points": 600,
    "vipLevel": "VIP1"
  }
}
```

---

### 1.8 每日签到页 (pages/member/sign/sign)

#### 前端功能需求
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 积分展示 | 显示当前积分余额 | `/api/member/sign/status` |
| 签到日历 | 显示当月日期，已签到标记 | `/api/member/sign/status` |
| 今日奖励 | 显示签到可获积分 | `/api/member/sign/status` |
| 连续签到福利 | 显示连续签到奖励规则 | `/api/member/sign/rewards` |
| 签到按钮 | 点击签到，签过则禁用 | `/api/member/sign` |

#### 后端接口详细设计

**1. 获取签到状态 - GET /api/member/sign/status**
```json
{
  "code": 200,
  "data": {
    "todaySigned": false,
    "continuousDays": 5,
    "totalPoints": 500,
    "dailyPoints": 5,
    "signCalendars": [
      {"date": "2024-01-25", "signed": true, "points": 5},
      {"date": "2024-01-26", "signed": true, "points": 5},
      {"date": "2024-01-27", "signed": true, "points": 5},
      {"date": "2024-01-28", "signed": true, "points": 5},
      {"date": "2024-01-29", "signed": false, "points": 5}
    ]
  }
}
```

**2. 获取连续签到奖励 - GET /api/member/sign/rewards**
```json
{
  "code": 200,
  "data": {
    "list": [
      {"days": 3, "reward": "额外 10 积分", "completed": true},
      {"days": 7, "reward": "额外 30 积分", "completed": false},
      {"days": 15, "reward": "额外 50 积分", "completed": false},
      {"days": 30, "reward": "额外 100 积分", "completed": false}
    ]
  }
}
```

**3. 执行签到 - POST /api/member/sign**
```json
{
  "code": 200,
  "message": "签到成功",
  "data": {
    "points": 5,
    "continuousDays": 6,
    "totalPoints": 505,
    "bonusPoints": 0
  }
}
```

---

### 1.9 积分兑换页 (pages/member/exchange/exchange)

#### 前端功能需求
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 积分展示 | 显示当前积分余额 | `/api/member/info` |
| 商品列表 | 显示可用积分兑换的商品 | `/api/member/points-products` |
| 兑换功能 | 积分足够时点击兑换 | `/api/member/points/exchange` |

#### 后端接口详细设计

**1. 获取积分商品列表 - GET /api/member/points-products**
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "name": "婴儿湿巾",
        "image": "https://xxx/wetwipes.jpg",
        "points": 100,
        "stock": 50
      }
    ],
    "total": 20,
    "page": 1,
    "pageSize": 20
  }
}
```

**2. 积分兑换 - POST /api/member/points/exchange**
请求：`{"productId": 1, "addressId": 1}`
```json
{
  "code": 200,
  "message": "兑换成功",
  "data": {
    "exchangeId": 1,
    "productName": "婴儿湿巾",
    "points": 100,
    "remainingPoints": 400,
    "status": "pending"
  }
}
```

---

### 1.10 服务详情页 (pages/service/*)

#### 前端功能需求（以婴儿游泳为例）

| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 服务轮播图 | 展示服务图片 | `/api/services/{id}` |
| 服务介绍 | 显示名称、价格、特点 | `/api/services/{id}` |
| 日期选择 | 显示可预约日期（未来7天） | `/api/services/{id}/available-dates` |
| 时段选择 | 显示当天可用时段（已预约不可选） | `/api/services/{id}/available-times?date=xxx` |
| 宝宝信息 | 输入宝宝姓名、月龄 | - |
| 联系信息 | 输入联系人姓名、手机号 | - |
| 提交预约 | 提交预约信息 | `/api/services/book` |

#### 后端接口详细设计

**1. 获取服务详情 - GET /api/services/{id}**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "name": "婴儿游泳",
    "icon": "🛁",
    "price": 58.00,
    "description": "专业婴儿洗澡服务...",
    "features": [
      {"icon": "🛁", "text": "专业洗护用品"},
      {"icon": "👩‍⚕️", "text": "经验丰富护理人员"}
    ],
    "images": [
      "https://xxx/swim1.jpg",
      "https://xxx/swim2.jpg"
    ],
    "packages": [
      {"id": 1, "name": "单次", "price": 58.00},
      {"id": 2, "name": "10次卡", "price": 520.00}
    ]
  }
}
```

**2. 获取可用日期 - GET /api/services/{id}/available-dates**
```json
{
  "code": 200,
  "data": {
    "list": [
      {"date": "2024-02-01", "week": "周四", "available": true},
      {"date": "2024-02-02", "week": "周五", "available": true},
      {"date": "2024-02-03", "week": "周六", "available": false}
    ]
  }
}
```

**3. 获取可用时段 - GET /api/services/{id}/available-times?date=2024-02-01**
```json
{
  "code": 200,
  "data": {
    "list": [
      {"time": "09:00-10:00", "available": true},
      {"time": "10:00-11:00", "available": false},
      {"time": "11:00-12:00", "available": true},
      {"time": "14:00-15:00", "available": true}
    ]
  }
}
```

**4. 提交预约 - POST /api/services/book**
请求：
```json
{
  "serviceId": 1,
  "packageId": 1,
  "babyName": "宝宝",
  "babyAge": "1岁",
  "bookingDate": "2024-02-01",
  "bookingTime": "09:00-10:00",
  "contactName": "张三",
  "contactPhone": "13800138000",
  "remark": ""
}
```
响应：
```json
{
  "code": 200,
  "message": "预约成功",
  "data": {
    "bookingId": 1,
    "bookingNo": "BK20240129001",
    "serviceName": "婴儿游泳",
    "bookingDate": "2024-02-01",
    "bookingTime": "09:00-10:00"
  }
}
```

---

### 1.11 消息通知页 (pages/message/message)

#### 前端功能需求
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 消息分类 | 全部/系统/订单/活动 | `/api/messages?type=xxx` |
| 消息列表 | 显示标题、内容、图标、时间、已读状态 | `/api/messages` |
| 标记已读 | 点击标记单条已读 | `/api/messages/{id}/read` |
| 全部已读 | 一键标记全部已读 | `/api/messages/read-all` |
| 删除消息 | 左滑删除消息 | `/api/messages/{id}` |

#### 后端接口详细设计

**1. 获取消息列表 - GET /api/messages**
参数：`type`(system/order/activity), `page`, `pageSize`
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "title": "订单已发货",
        "content": "您的订单已发货，请注意查收",
        "type": "order",
        "icon": "📦",
        "isRead": false,
        "createTime": "2024-01-29 14:00:00"
      }
    ],
    "total": 10,
    "unreadCount": 3,
    "page": 1,
    "pageSize": 20
  }
}
```

**2. 标记已读 - PUT /api/messages/{id}/read**
```json
{
  "code": 200,
  "message": "标记成功"
}
```

**3. 全部已读 - PUT /api/messages/read-all**
```json
{
  "code": 200,
  "message": "标记成功"
}
```

**4. 删除消息 - DELETE /api/messages/{id}**
```json
{
  "code": 200,
  "message": "删除成功"
}
```

---

### 1.12 登录注册页

#### 前端功能需求

**登录页 (pages/login/login)**
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 用户名输入 | 输入用户名 | - |
| 密码输入 | 输入密码 | - |
| 登录按钮 | 调用登录接口 | `/api/auth/login` |
| 注册链接 | 跳转注册页 | `/pages/register/register` |

**注册页 (pages/register/register)**
| 功能模块 | 具体需求 | 后端接口 |
|---------|---------|---------|
| 用户名输入 | 输入用户名 | - |
| 密码输入 | 输入密码 | - |
| 确认密码 | 再次输入密码 | - |
| 注册按钮 | 调用注册接口 | `/api/auth/register` |
| 登录链接 | 已有账号去登录 | `/pages/login/login` |

#### 后端接口详细设计

**1. 用户登录 - POST /api/auth/login**
请求：`{"username": "user123", "password": "xxx123"}`
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "user123",
      "nickname": "宝宝妈妈",
      "avatar": "",
      "vipLevel": "VIP0",
      "points": 0,
      "balance": 0.00
    }
  }
}
```

**2. 用户注册 - POST /api/auth/register**
请求：`{"username": "user123", "password": "xxx123", "phone": "13800138000"}`
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": 1
  }
}
```

---

## 二、数据库设计

### 2.1 核心数据表

#### 用户表 (users)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| username | varchar(50) | 用户名（唯一） |
| password | varchar(255) | BCrypt加密密码 |
| nickname | varchar(50) | 昵称 |
| avatar | varchar(500) | 头像URL |
| phone | varchar(20) | 手机号 |
| vip_level | varchar(20) | VIP等级 |
| points | int | 积分余额 |
| balance | decimal(10,2) | 账户余额 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

#### 商品表 (products)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| category_id | bigint | 分类ID |
| name | varchar(100) | 商品名称 |
| images | text | JSON数组：多图 |
| detail_images | text | JSON数组：详情图 |
| price | decimal(10,2) | 售价 |
| original_price | decimal(10,2) | 原价 |
| spec | varchar(100) | 规格 |
| stock | int | 库存 |
| sales | int | 销量 |
| is_seckill | tinyint | 是否秒杀 |
| is_hot | tinyint | 是否爆款 |
| status | tinyint | 状态(0下架1上架) |
| created_at | datetime | 创建时间 |

#### 订单表 (orders)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| order_no | varchar(50) | 订单号（唯一） |
| user_id | bigint | 用户ID |
| total_price | decimal(10,2) | 总价 |
| status | varchar(20) | 订单状态 |
| pay_method | varchar(20) | 支付方式 |
| pay_time | datetime | 支付时间 |
| address_id | bigint | 收货地址ID |
| remark | varchar(200) | 备注 |
| created_at | datetime | 创建时间 |

#### 订单商品表 (order_items)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| order_id | bigint | 订单ID |
| product_id | bigint | 商品ID |
| package_id | bigint | 套餐ID |
| product_name | varchar(100) | 商品名称快照 |
| product_image | varchar(200) | 商品图片快照 |
| price | decimal(10,2) | 购买单价 |
| quantity | int | 数量 |

#### 收藏表 (favorites)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户ID |
| product_id | bigint | 商品ID |
| created_at | datetime | 创建时间 |

**唯一索引**：(user_id, product_id)

#### 购物车表 (cart_items)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户ID |
| product_id | bigint | 商品ID |
| package_id | bigint | 套餐ID |
| quantity | int | 数量 |
| created_at | datetime | 创建时间 |

#### 签到记录表 (sign_records)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户ID |
| sign_date | date | 签到日期 |
| points | int | 获得积分 |
| bonus_points | int | 额外奖励积分 |
| created_at | datetime | 创建时间 |

**唯一索引**：(user_id, sign_date)

#### 服务预约表 (service_bookings)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| booking_no | varchar(50) | 预约号 |
| user_id | bigint | 用户ID |
| service_id | bigint | 服务ID |
| package_id | bigint | 套餐ID |
| baby_name | varchar(50) | 宝宝姓名 |
| baby_age | varchar(20) | 宝宝月龄 |
| contact_name | varchar(50) | 联系人 |
| contact_phone | varchar(20) | 联系电话 |
| booking_date | date | 预约日期 |
| booking_time | varchar(50) | 预约时段 |
| status | varchar(20) | 状态 |
| remark | varchar(200) | 备注 |
| created_at | datetime | 创建时间 |

#### 消息表 (messages)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户ID（0为系统消息） |
| title | varchar(100) | 标题 |
| content | text | 内容 |
| type | varchar(20) | 类型(system/order/activity) |
| icon | varchar(20) | 图标emoji |
| is_read | tinyint | 是否已读 |
| created_at | datetime | 创建时间 |

#### 会员档位表 (member_tiers)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| name | varchar(50) | 档位名称 |
| amount | decimal(10,2) | 充值金额 |
| bonus | decimal(10,2) | 赠送金额 |
| vip_level | varchar(20) | 升级VIP等级 |
| points_bonus | int | 赠送积分 |
| sort_order | int | 排序 |

#### 充值记录表 (recharge_records)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户ID |
| tier_id | bigint | 档位ID |
| amount | decimal(10,2) | 充值金额 |
| bonus | decimal(10,2) | 赠送金额 |
| transaction_no | varchar(50) | 交易流水号 |
| status | varchar(20) | 状态 |
| created_at | datetime | 创建时间 |

#### 积分商品表 (points_products)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| name | varchar(100) | 商品名称 |
| image | varchar(200) | 图片 |
| points | int | 所需积分 |
| stock | int | 库存 |
| status | tinyint | 状态 |
| created_at | datetime | 创建时间 |

#### 积分兑换记录表 (points_exchanges)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户ID |
| product_id | bigint | 商品ID |
| points | int | 消耗积分 |
| address_id | bigint | 收货地址ID |
| status | varchar(20) | 状态 |
| created_at | datetime | 创建时间 |

---

## 三、业务逻辑详细说明

### 3.1 认证流程

1. **登录流程**
   - 用户输入用户名密码
   - 后端验证密码（BCrypt）
   - 生成JWT token返回
   - 前端存储token到Storage
   - 后续请求带上Authorization头

2. **注册流程**
   - 用户输入用户名、密码、手机号
   - 后端验证用户名唯一性
   - 密码BCrypt加密存储
   - 返回用户ID

### 3.2 订单流程

1. **创建订单**
   - 校验库存是否充足
   - 生成唯一订单号（时间戳+随机数）
   - 扣减库存
   - 记录订单商品快照（名称、图片、价格）
   - 返回订单信息

2. **订单状态流转**
   ```
   pending_payment → paid → shipped → completed
        ↓              ↓        ↓
     cancelled    cancelled   refunded
   ```

3. **取消订单**
   - 仅待付款状态可取消
   - 恢复库存
   - 更新订单状态

4. **支付流程**
   - 接收支付方式参数
   - 调用支付接口（模拟）
   - 更新订单状态为已支付
   - 发送消息通知

### 3.3 会员积分流程

1. **签到逻辑**
   - 校验今日是否已签到
   - 计算连续签到天数
   - 基础积分 + 连续签到奖励
   - 记录签到日志
   - 返回本次获得积分数

2. **充值逻辑**
   - 校验档位是否存在
   - 计算到账金额（含赠送）
   - 更新用户余额和VIP等级
   - 记录充值流水

### 3.4 服务预约流程

1. **预约逻辑**
   - 校验预约日期是否在可预约范围内
   - 校验时段是否已被预约
   - 生成预约号
   - 记录预约信息
   - 发送预约成功消息

2. **预约冲突处理**
   - 使用数据库事务
   - 锁定时段记录
   - 预约成功后标记时段不可用

### 3.5 消息通知机制

1. **消息类型**
   - system：系统消息（全员推送）
   - order：订单消息（用户专属）
   - activity：活动消息（用户专属）

2. **触发场景**
   - 下单成功 → 订单消息
   - 支付成功 → 订单消息
   - 发货通知 → 订单消息
   - 会员充值 → 会员消息
   - 积分变动 → 会员消息
   - 系统公告 → 系统消息

---

## 四、错误码规范

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权（token无效或过期） |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 409 | 冲突（如重复收藏） |
| 422 | 业务逻辑错误（如库存不足、余额不足） |
| 500 | 服务器内部错误 |

---

## 五、通用响应格式

### 成功响应
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 错误响应
```json
{
  "code": 400,
  "message": "请求参数错误",
  "errors": {
    "username": "用户名不能为空"
  }
}
```

---

## 六、安全与性能建议

### 6.1 安全建议
1. 密码BCrypt加密存储
2. JWT token有效期设为7天
3. 敏感接口需验证token
4. SQL参数化查询防注入
5. 统一输入验证与输出转义

### 6.2 性能建议
1. 商品列表接口添加Redis缓存（5分钟）
2. 首页数据缓存（1分钟）
3. 数据库添加必要索引
4. 图片使用CDN加速

---

## 七、前端页面路由对照表

| 页面路径 | 对应功能 |
|---------|---------|
| /pages/index/index | 首页 |
| /pages/products/index | 商品列表 |
| /pages/product-detail/index | 商品详情 |
| /pages/hot/index | 热门商品 |
| /pages/profile/index | 个人中心 |
| /pages/profile/orders | 我的订单 |
| /pages/profile/favorites | 我的收藏 |
| /pages/login/login | 登录 |
| /pages/register/register | 注册 |
| /pages/message/message | 消息通知 |
| /pages/member/recharge/recharge | 会员充值 |
| /pages/member/sign/sign | 每日签到 |
| /pages/member/exchange/exchange | 积分兑换 |
| /pages/service/bath/bath | 婴儿游泳 |
| /pages/service/hair/hair | 婴儿理发 |
| /pages/service/photo/photo | 满月照 |
| /pages/service/playground/playground | 游乐园 |
| /pages/service/early-education/early-education | 早教课程 |
| /pages/service/dad/dad | 奶爸服务 |

---

## 八、部署清单

1. **服务器要求**
   - CPU: 2核+
   - 内存: 4GB+
   - 硬盘: 50GB+

2. **软件环境**
   - JDK 11+（Java项目）
   - MySQL 8.0+
   - Redis 6.0+（可选）
   - Nginx（反向代理）

3. **微信小程序配置**
   - 在微信公众平台配置服务器域名
   - 设置业务域名（需HTTPS）
   - 配置AppID和AppSecret
